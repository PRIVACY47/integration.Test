using System.Security.Cryptography.X509Certificates;

namespace KekaAdapter.Service
{
    public class KekaAdapterService
    {
        public KekaAdapterService() 
        {
            
             
        }


        public string GetUtcFromKeka(string data)
        {
            // make api call
            //inside api call first generate token on sucess only make api call
            //handle pagination

            //handle all the mappping using the auto mappter


            return data;
        }
    }
}